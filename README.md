# Darkmode

* Start webserver met: `npx browser-sync start --server --files "index.html, **/*.css, **/*.js"`
* button_mode:
    * Gebruik javascript om via `data-theme` te switchen tussen dark-, light- en systemmodel
* system_mode:
    * Gebruik media-query om `prefers-color-scheme` in te stellen.